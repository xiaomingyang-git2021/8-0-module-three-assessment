import React from "react";

class Home extends React.Component {
    // can all the fetching happen on Home based on the Route(endpoint of the API link) user clicks on

    render() {
        return (
        <h1>Welcome to GhibliApp</h1>
        )
    }
}

export default Home;